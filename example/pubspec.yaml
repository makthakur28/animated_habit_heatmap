name: animated_habit_heatmap_example
description: "Example app showcasing the animated_habit_heatmap package"
version: 1.0.0

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  animated_habit_heatmap:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

